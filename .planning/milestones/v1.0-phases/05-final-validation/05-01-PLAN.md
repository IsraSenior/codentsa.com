---
phase: 05-final-validation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .claude/rules/architecture.md
  - .claude/rules/conventions.md
autonomous: true
requirements:
  - DOCS-04
must_haves:
  truths:
    - "CLAUDE.md rule files accurately document the component hierarchy as layout/ui/features"
    - "CLAUDE.md rule files show Nuxt 4.3.1 as the stack version"
    - "Every component listed in conventions.md exists at the documented path"
  artifacts:
    - path: ".claude/rules/architecture.md"
      provides: "Accurate stack version and project architecture"
      contains: "Nuxt 4.3.1"
    - path: ".claude/rules/conventions.md"
      provides: "Accurate component structure documentation"
      contains: "layout/"
  key_links:
    - from: "CLAUDE.md"
      to: ".claude/rules/architecture.md"
      via: "markdown link"
      pattern: "architecture\\.md"
    - from: "CLAUDE.md"
      to: ".claude/rules/conventions.md"
      via: "markdown link"
      pattern: "conventions\\.md"
---

<objective>
Update .claude/rules/ files so CLAUDE.md accurately reflects the project after all Phase 1-4 changes.

Purpose: DOCS-04 requires CLAUDE.md to be the single source of truth. The root CLAUDE.md is already a lightweight index (from Phase 3), but two rule files contain outdated information: architecture.md says "Nuxt 4" instead of "Nuxt 4.3.1", and conventions.md still describes the old flat component structure instead of the new layout/ui/features hierarchy.

Output: Updated architecture.md and conventions.md that match the actual codebase.
</objective>

<execution_context>
@/Users/israsenior/.claude/get-shit-done/workflows/execute-plan.md
@/Users/israsenior/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.claude/rules/architecture.md
@.claude/rules/conventions.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update architecture.md with accurate Nuxt version</name>
  <files>.claude/rules/architecture.md</files>
  <action>
In .claude/rules/architecture.md, update the Stack line from:
- **Stack**: Nuxt 4 + Vue 3 + Tailwind CSS v4 + Pinia, JavaScript (no TypeScript)
to:
- **Stack**: Nuxt 4.3.1 + Vue 3 + Tailwind CSS v4 + Pinia, JavaScript (no TypeScript)

No other changes needed in this file -- the rest of the architecture description (frontend structure, backend, stores, data flow) is still accurate.
  </action>
  <verify>
    <automated>grep "Nuxt 4.3.1" .claude/rules/architecture.md</automated>
  </verify>
  <done>architecture.md Stack line says "Nuxt 4.3.1" matching the actual installed version</done>
</task>

<task type="auto">
  <name>Task 2: Update conventions.md with new component hierarchy</name>
  <files>.claude/rules/conventions.md</files>
  <action>
In .claude/rules/conventions.md, replace the entire "## Componentes y Paginas" section. The current text says:

**Componentes** (estructura flat por dominio): Product/, Cart/, Checkout/, Toast/, Bento/, Brand/, Staff/, FAQ/, Form/, Legal/

Replace with the actual hierarchy after Phase 4 reorganization:

**Componentes** (estructura jerarquica en `app/components/`):
- `layout/`: AnnouncementBar, Footer, Header, HeroBanner, Logo, Section
- `ui/`: BaseCarousel, Breadcrumbs, Button, DateInput, OfferPopup, Pagination
- `features/`: Bento/, Brand/, Cart/, Checkout/, FAQ/, Legal/, Product/, Staff/, Testimonials/, Toast/

Keep the "Auto-imports de Nuxt activos" note and the entire "Paginas principales" subsection unchanged -- those are still accurate.

Do NOT add Form/ (it does not exist in the reorganized structure). Do NOT omit Testimonials/ (it was added during reorg). Do NOT omit Pagination (it exists in ui/).
  </action>
  <verify>
    <automated>grep -c "layout/\|ui/\|features/" .claude/rules/conventions.md | grep -q "3" && echo "PASS: all three hierarchy levels documented" || echo "FAIL"</automated>
  </verify>
  <done>conventions.md lists the exact component hierarchy matching app/components/ on disk: 6 layout components, 6 ui components, 10 feature subdirectories</done>
</task>

</tasks>

<verification>
After both tasks, run:
1. `grep "Nuxt 4.3.1" .claude/rules/architecture.md` -- confirms version updated
2. `grep -A3 "layout/" .claude/rules/conventions.md` -- confirms hierarchy documented
3. `pnpm build` -- confirms no accidental breakage from doc edits (safety check)
</verification>

<success_criteria>
- architecture.md Stack line reads "Nuxt 4.3.1" (not "Nuxt 4")
- conventions.md documents layout/ui/features hierarchy with all actual components
- No component listed in conventions.md is missing from disk
- No component on disk is missing from conventions.md
</success_criteria>

<output>
After completion, create `.planning/phases/05-final-validation/05-01-SUMMARY.md`
</output>
