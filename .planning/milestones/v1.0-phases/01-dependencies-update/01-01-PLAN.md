---
phase: 01-dependencies-update
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - pnpm-lock.yaml
  - nuxt.config.ts
autonomous: true
requirements:
  - DEPS-01
  - DEPS-02

must_haves:
  truths:
    - "Nuxt version is 4.3.1 in package.json and dev server output"
    - "All dependencies are at latest compatible versions"
    - "No major-version bumps that break Nuxt 4.x compatibility (vue-router stays v4, eslint stays v9)"
  artifacts:
    - path: "package.json"
      provides: "Updated dependency versions"
      contains: "nuxt.*4.3"
    - path: "pnpm-lock.yaml"
      provides: "Locked dependency tree"
  key_links:
    - from: "package.json"
      to: "nuxt.config.ts"
      via: "module resolution"
      pattern: "nuxt|tailwindcss|@pinia/nuxt"
---

<objective>
Update Nuxt to 4.3.1 and all other dependencies to their latest compatible versions.

Purpose: Establish a stable, current foundation before any code changes in later phases.
Output: Updated package.json and pnpm-lock.yaml with all deps at latest compatible versions.
</objective>

<execution_context>
@/Users/israsenior/.claude/get-shit-done/workflows/execute-plan.md
@/Users/israsenior/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@package.json
@nuxt.config.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update Nuxt and all safe dependencies</name>
  <files>package.json, pnpm-lock.yaml</files>
  <action>
Update dependencies in this specific order to avoid conflicts:

1. Update Nuxt first (the core framework):
   - `pnpm update nuxt` (should resolve to 4.3.1 given ^4.2.1 range)

2. Update Tailwind ecosystem together:
   - `pnpm update tailwindcss @tailwindcss/vite` (4.1.17 -> 4.2.x)

3. Update Vue ecosystem (CAREFUL with vue-router):
   - `pnpm update vue` (3.5.24 -> 3.5.29)
   - Do NOT update vue-router to v5.x -- it is a major version bump. Keep the ^4.6.3 range. Run `pnpm update vue-router` which should stay within v4.x range.

4. Update Pinia:
   - `pnpm update pinia @pinia/nuxt`

5. Update UI/utility packages:
   - `pnpm update swiper @heroicons/vue`

6. Update @directus/sdk:
   - `pnpm update @directus/sdk` -- this will go from 20.x to 21.x (major bump). Check the Directus SDK changelog: v21 changed import paths. After updating, verify that `server/utils/directus.js` and `app/composables/useDirectus.js` still use valid imports. If the SDK v21 has breaking import changes, pin to ^20.1.1 instead and add a note.

7. Update analytics:
   - `pnpm update nuxt-gtag`

8. Update dev dependencies (CAREFUL with eslint):
   - `pnpm update @nuxt/eslint eslint-plugin-vue prettier nuxt-umami`
   - Do NOT update eslint to v10.x -- it is a major version bump that may break @nuxt/eslint compatibility. Keep ^9.x range. If `pnpm update eslint` tries to go to v10, pin it: `pnpm add -D eslint@^9.39.1`

9. After all updates, verify package.json shows expected versions and run `pnpm install` to ensure lockfile is clean.

IMPORTANT version constraints:
- vue-router: MUST stay on v4.x (v5 is incompatible with Nuxt 4.x)
- eslint: MUST stay on v9.x (v10 may break @nuxt/eslint)
- Everything else: latest within semver range is fine
  </action>
  <verify>
    <automated>pnpm install --frozen-lockfile 2>&1 | tail -5 && node -e "const p=require('./package.json'); console.log('nuxt:', p.dependencies.nuxt); console.log('vue-router:', p.dependencies['vue-router']); console.log('eslint:', p.devDependencies.eslint)"</automated>
  </verify>
  <done>package.json shows nuxt ^4.3.x (resolved to 4.3.1), vue-router stays ^4.x, eslint stays ^9.x, all other deps at latest compatible versions. pnpm-lock.yaml is consistent.</done>
</task>

<task type="auto">
  <name>Task 2: Verify Directus SDK and nuxt.config compatibility</name>
  <files>server/utils/directus.js, app/composables/useDirectus.js, nuxt.config.ts</files>
  <action>
After dependency updates from Task 1, verify that code still works with new versions:

1. Read `server/utils/directus.js` and `app/composables/useDirectus.js`. Check their import statements against the installed @directus/sdk version. If @directus/sdk was updated to v21, verify imports match v21 API. Common v21 change: `createDirectus` replaces `Directus` constructor, and transport/rest imports moved. Fix any breaking import paths.

2. Read `nuxt.config.ts`. Verify all modules listed still work with updated versions:
   - `@pinia/nuxt` -- should be fine
   - `nuxt-gtag` -- should be fine
   - `nuxt-umami` -- verify config shape hasn't changed
   - `@nuxt/eslint` -- should be fine

3. Run `pnpm dev` and verify it starts without errors (let it boot, check for module resolution errors in the first 10 seconds of output, then kill it).

4. If any import or config issues found, fix them minimally -- only what's needed to restore compatibility, no refactoring.
  </action>
  <verify>
    <automated>timeout 30 pnpm dev 2>&1 | head -30</automated>
  </verify>
  <done>Dev server starts successfully with Nuxt 4.3.1, no module resolution errors, no deprecation warnings from updated packages.</done>
</task>

</tasks>

<verification>
- `pnpm outdated` shows no updates available (except any intentionally pinned like vue-router v4, eslint v9)
- `pnpm dev` starts and reports Nuxt 4.3.1
- No import or module resolution errors in server output
</verification>

<success_criteria>
- Nuxt version is 4.3.1
- All dependencies updated to latest compatible versions
- vue-router remains on v4.x, eslint on v9.x
- Dev server starts without errors
</success_criteria>

<output>
After completion, create `.planning/phases/01-dependencies-update/01-01-SUMMARY.md`
</output>
