---
phase: 03-claude-config
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .claude/rules/architecture.md
  - .claude/rules/design-tokens.md
  - .claude/rules/conventions.md
  - .claude/settings.json
  - CLAUDE.md
autonomous: true
requirements: [CLDE-01, CLDE-02, CLDE-03, CLDE-04]

must_haves:
  truths:
    - "Claude Code loads modular rules from .claude/rules/ instead of a monolithic CLAUDE.md"
    - "Project settings.json configures permissions and MCP servers correctly"
    - "Playwright MCP screenshots write to .playwright-mcp/ not project root"
    - "CLAUDE.md is a lightweight index pointing to .claude/rules/ for details"
  artifacts:
    - path: ".claude/rules/architecture.md"
      provides: "Stack, frontend/backend structure, data flow, stores, payment flow, CMS integration, analytics"
      contains: "Nuxt 4"
    - path: ".claude/rules/design-tokens.md"
      provides: "Typography (font-title, font-body), color scales (primary, secondary, neutral, system), usage examples"
      contains: "primary-500"
    - path: ".claude/rules/conventions.md"
      provides: "Prettier config, ESLint rules, commit conventions, security rules, agent table, component structure, pages list"
      contains: "conventional commits"
    - path: ".claude/settings.json"
      provides: "Permissions allow list and MCP server config with Playwright output dir"
      contains: "playwright"
    - path: "CLAUDE.md"
      provides: "Lightweight index with dev commands and pointers to .claude/rules/"
      min_lines: 15
  key_links:
    - from: "CLAUDE.md"
      to: ".claude/rules/architecture.md"
      via: "markdown reference"
      pattern: "\\.claude/rules/architecture\\.md"
    - from: "CLAUDE.md"
      to: ".claude/rules/design-tokens.md"
      via: "markdown reference"
      pattern: "\\.claude/rules/design-tokens\\.md"
    - from: "CLAUDE.md"
      to: ".claude/rules/conventions.md"
      via: "markdown reference"
      pattern: "\\.claude/rules/conventions\\.md"
---

<objective>
Set up the .claude/ directory with modular rules extracted from CLAUDE.md, configure project settings.json with permissions and Playwright MCP output directory, and slim down CLAUDE.md to a lightweight index.

Purpose: Replace the monolithic 143-line CLAUDE.md with organized, modular rule files that Claude Code loads automatically from .claude/rules/, and ensure tool configuration is correct.
Output: Three rule files in .claude/rules/, a .claude/settings.json, and a slim CLAUDE.md index.
</objective>

<execution_context>
@/Users/israsenior/.claude/get-shit-done/workflows/execute-plan.md
@/Users/israsenior/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@CLAUDE.md
@.claude/settings.local.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create modular rule files in .claude/rules/</name>
  <files>.claude/rules/architecture.md, .claude/rules/design-tokens.md, .claude/rules/conventions.md</files>
  <action>
Create directory .claude/rules/ and extract content from the current CLAUDE.md into three focused rule files:

1. `.claude/rules/architecture.md` — Extract the "Arquitectura" and "Flujo de Datos" sections verbatim. Include:
   - Stack definition (Nuxt 4 + Vue 3 + Tailwind CSS v4 + Pinia, JavaScript)
   - Frontend/Backend/Estado/Productos/Persistencia/Pagos/CMS/Analytics bullet points
   - Full "Flujo de Datos" section (Productos, Carrito, Pagos, Directus flows)

2. `.claude/rules/design-tokens.md` — Extract the entire "Tokens de Diseno" section verbatim. Include:
   - Tipografias table (font-title Figtree, font-body Blauer Nue)
   - All color tables: Primary (rojo), Secondary (azul), Neutral (gris-teal), Sistema
   - The note about `--color-*: initial` resetting Tailwind colors
   - "Uso de Tokens" code example

3. `.claude/rules/conventions.md` — Extract "Convenciones de Codigo", "Agentes Especializados", and "Componentes y Paginas" sections. Include:
   - Prettier/ESLint/Commits/Security rules
   - Full "Agentes Especializados (OBLIGATORIO)" table with the instruction to always use specialized agents
   - Component structure (flat por dominio) and full pages list

Each file should start with a brief title comment (e.g., `# Architecture`) but otherwise preserve the original content formatting exactly. Do NOT add or remove information — this is a reorganization, not a rewrite.
  </action>
  <verify>
    <automated>test -f .claude/rules/architecture.md && test -f .claude/rules/design-tokens.md && test -f .claude/rules/conventions.md && grep -q "Nuxt 4" .claude/rules/architecture.md && grep -q "primary-500" .claude/rules/design-tokens.md && grep -q "conventional commits" .claude/rules/conventions.md && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <done>Three rule files exist in .claude/rules/ containing all content previously in CLAUDE.md sections, with no information lost</done>
</task>

<task type="auto">
  <name>Task 2: Create .claude/settings.json with permissions and Playwright MCP config</name>
  <files>.claude/settings.json</files>
  <action>
Create `.claude/settings.json` (project-level, separate from the existing settings.local.json) with:

1. **Permissions allow list** — Clean, project-relevant permissions (not the accumulated mess in settings.local.json):
   ```json
   "permissions": {
     "allow": [
       "Bash(pnpm dev:*)",
       "Bash(pnpm build:*)",
       "Bash(pnpm lint:*)",
       "Bash(pnpm format:*)",
       "Bash(pnpm generate:*)",
       "Bash(pnpm preview:*)",
       "Bash(git add:*)",
       "Bash(git commit:*)",
       "Bash(git log:*)",
       "Bash(git diff:*)",
       "Bash(git status:*)",
       "Bash(git rm:*)",
       "Bash(ls:*)",
       "Bash(tree:*)",
       "Bash(node:*)"
     ]
   }
   ```

2. **MCP servers** — Configure Playwright MCP to output screenshots to `.playwright-mcp/` directory:
   ```json
   "mcpServers": {
     "playwright": {
       "command": "npx",
       "args": ["@anthropic-ai/mcp-playwright", "--output-dir", ".playwright-mcp"]
     }
   }
   ```

Note: settings.local.json already exists and contains user-specific permissions and plugin config. The new settings.json is the project-level shared config that gets committed to git. It should NOT duplicate settings.local.json content — settings.json has project defaults, settings.local.json has user overrides.
  </action>
  <verify>
    <automated>test -f .claude/settings.json && python3 -c "import json; d=json.load(open('.claude/settings.json')); assert 'permissions' in d; assert 'mcpServers' in d; assert '.playwright-mcp' in str(d['mcpServers']); print('PASS')" || echo "FAIL"</automated>
  </verify>
  <done>.claude/settings.json exists with clean permissions and Playwright MCP configured to output to .playwright-mcp/</done>
</task>

<task type="auto">
  <name>Task 3: Rewrite CLAUDE.md as lightweight index</name>
  <files>CLAUDE.md</files>
  <action>
Replace the current 143-line CLAUDE.md with a lightweight index (~30-40 lines) that:

1. Keeps the "Comandos de Desarrollo" section (the pnpm commands block) — this is useful quick-reference that belongs at the top level.

2. Adds a "Rules" section pointing to the modular files:
   ```markdown
   ## Rules

   Detailed project rules are in `.claude/rules/`:

   - **[Architecture](.claude/rules/architecture.md)** — Stack, structure, data flow, integrations
   - **[Design Tokens](.claude/rules/design-tokens.md)** — Typography, colors, usage
   - **[Conventions](.claude/rules/conventions.md)** — Code style, agents, components, pages
   ```

3. Keeps a brief one-line project description at the top: "This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository."

4. Does NOT include any of the detailed content that now lives in .claude/rules/ — no architecture details, no color tables, no agent tables, no component lists.

The result should be a file that loads fast, gives essential commands, and directs Claude to the modular rules for everything else.
  </action>
  <verify>
    <automated>test -f CLAUDE.md && wc -l < CLAUDE.md | awk '{if ($1 < 50) print "PASS: "$1" lines"; else print "FAIL: "$1" lines (should be under 50)"}' && grep -q ".claude/rules/architecture.md" CLAUDE.md && grep -q ".claude/rules/design-tokens.md" CLAUDE.md && grep -q ".claude/rules/conventions.md" CLAUDE.md && echo "REFS_PASS" || echo "REFS_FAIL"</automated>
  </verify>
  <done>CLAUDE.md is under 50 lines, contains dev commands, and references all three .claude/rules/ files</done>
</task>

</tasks>

<verification>
All phase success criteria verified:
1. `.claude/rules/` contains architecture.md, design-tokens.md, conventions.md
2. `.claude/settings.json` has permissions and MCP server config
3. Playwright MCP configured with `--output-dir .playwright-mcp`
4. CLAUDE.md is a lightweight index under 50 lines referencing .claude/rules/
</verification>

<success_criteria>
- Three modular rule files exist in .claude/rules/ with complete content from CLAUDE.md
- .claude/settings.json is valid JSON with permissions and mcpServers
- Playwright output dir set to .playwright-mcp/
- CLAUDE.md is under 50 lines and references all three rule files
- No information lost from the original CLAUDE.md (all content preserved in rule files)
</success_criteria>

<output>
After completion, create `.planning/phases/03-claude-config/03-01-SUMMARY.md`
</output>
