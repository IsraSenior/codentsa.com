---
phase: 02-docs-cleanup
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .gitignore
  - docs/architecture.md
  - docs/components.md
  - docs/directus-integration.md
autonomous: true
requirements: [DOCS-01, DOCS-02, DOCS-03]

must_haves:
  truths:
    - "No PNG files exist in the project root directory"
    - ".gitignore contains .playwright-mcp/ entry"
    - "docs/architecture.md accurately describes the current Nuxt 4 app structure and server layout"
    - "docs/components.md lists the actual 42 components in their current flat structure"
    - "docs/directus-integration.md reflects the actual composable and server utility code"
  artifacts:
    - path: ".gitignore"
      provides: "Playwright MCP output exclusion"
      contains: ".playwright-mcp"
    - path: "docs/architecture.md"
      provides: "Accurate project architecture documentation"
      min_lines: 50
    - path: "docs/components.md"
      provides: "Accurate component inventory"
      min_lines: 50
    - path: "docs/directus-integration.md"
      provides: "Accurate Directus integration documentation"
      min_lines: 50
  key_links:
    - from: "docs/architecture.md"
      to: "app/ and server/ directories"
      via: "file path references"
      pattern: "app/|server/"
    - from: "docs/components.md"
      to: "app/components/"
      via: "component file listings"
      pattern: "components/"
---

<objective>
Clean up stale documentation and remove loose files from the project root.

Purpose: The docs/ directory contains outdated documentation that references wrong file paths and structures. Three stray PNG screenshots sit in the project root. The .gitignore is missing a .playwright-mcp/ exclusion.
Output: Updated docs reflecting actual code, clean project root, complete .gitignore.
</objective>

<execution_context>
@/Users/israsenior/.claude/get-shit-done/workflows/execute-plan.md
@/Users/israsenior/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove root PNGs and update .gitignore</name>
  <files>.gitignore</files>
  <action>
1. Delete the 3 PNG files from the project root:
   - 02-producto-detalle-con-precio.png
   - 03-aviso-legal.png
   - 04-terminos-condiciones.png
   These are stray screenshots that do not belong in the repository root. They are untracked by git so simply delete them from disk.

2. Add `.playwright-mcp/` to .gitignore. Place it after the existing `.idea` entry in the "Misc" section. The .playwright-mcp/ directory already exists with a screenshots/ subdirectory — this entry prevents it from being tracked.

Do NOT modify any other .gitignore entries.
  </action>
  <verify>
    <automated>ls *.png 2>/dev/null | wc -l | grep -q '^0$' && grep -q '.playwright-mcp' .gitignore && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <done>Zero PNG files in project root. .gitignore contains .playwright-mcp/ entry.</done>
</task>

<task type="auto">
  <name>Task 2: Update stale documentation files</name>
  <files>docs/architecture.md, docs/components.md, docs/directus-integration.md</files>
  <action>
Update three documentation files to accurately reflect the current codebase. For each file, READ the current doc first, then READ the actual source files it describes, then REWRITE the doc to match reality.

**docs/architecture.md:**
- Read current doc, then inspect: app/ directory structure, server/ directory structure, nuxt.config.ts, package.json
- Update to reflect: Nuxt 4.3.1 (not older version), Vue 3.5.29, Tailwind CSS v4, Pinia stores
- Ensure file paths match actual structure: app/ (pages, components, composables, stores, assets, layouts), server/api/, server/utils/
- Document the actual Nitro server routes under server/api/ (especially redsys/)
- Keep the same general document style/format but make content accurate

**docs/components.md:**
- Read current doc, then list all files under app/components/ recursively
- Document the actual flat-by-domain structure: standalone .vue files (AnnouncementBar, BaseCarousel, Breadcrumbs, Button, Footer, Header, HeroBanner, Logo, OfferPopup, Pagination, Section) and domain directories (Bento/, Brand/, Cart/, Checkout/, FAQ/, Form/, Legal/, Product/, Staff/, Testimonials/, Toast/)
- For each domain directory, list the actual component files inside it
- Note: this is the PRE-reorganization structure (Phase 4 will restructure into ui/layout/features)

**docs/directus-integration.md:**
- Read current doc, then read: app/composables/useDirectus.js, server/utils/directus.js
- Update to reflect the actual composable API (functions exported, how they're called)
- Update to reflect actual server utility (how Directus SDK is initialized server-side)
- Note @directus/sdk is on v20.x (kept intentionally, see Phase 1 decisions)
- Ensure any code examples match the actual function signatures

Preserve the overall structure and purpose of each document. Do not add speculative content — only document what actually exists in the code.
  </action>
  <verify>
    <automated>test -f docs/architecture.md && test -f docs/components.md && test -f docs/directus-integration.md && grep -q "4.3.1\|Nuxt 4" docs/architecture.md && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <done>All three docs accurately describe the current codebase. architecture.md references Nuxt 4.3.1 and correct directory structure. components.md lists every component in app/components/. directus-integration.md matches the actual useDirectus.js and server/utils/directus.js code.</done>
</task>

</tasks>

<verification>
1. `ls *.png 2>/dev/null` returns nothing (no PNGs in root)
2. `grep '.playwright-mcp' .gitignore` returns a match
3. `grep -c '4.3.1\|Nuxt 4' docs/architecture.md` returns at least 1
4. `find app/components -name '*.vue' | wc -l` count matches number of components documented in docs/components.md
5. `grep -l 'useDirectus\|createDirectus' docs/directus-integration.md` returns a match
</verification>

<success_criteria>
- No PNG files in project root directory
- .gitignore includes .playwright-mcp/ entry
- docs/architecture.md references Nuxt 4.3.1, correct app/ and server/ structure
- docs/components.md lists all actual components in their current flat-by-domain structure
- docs/directus-integration.md matches the actual useDirectus composable and server utility code
</success_criteria>

<output>
After completion, create `.planning/phases/02-docs-cleanup/02-01-SUMMARY.md`
</output>
